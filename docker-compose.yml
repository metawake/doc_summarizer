services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - db_data:/app/data
      - uploads:/app/uploads
    environment:
      - DATABASE_URL=sqlite:///./data/documents.db

volumes:
  db_data:
  uploads:
